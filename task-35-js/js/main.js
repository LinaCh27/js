function numRange(n) {
    if (n >= 100 && n <= 200) {
        console.log("The number is in the range.");
    } else {
        console.log("The number is not in range.");
    }
    
}

numRange(127);
numRange(93);


